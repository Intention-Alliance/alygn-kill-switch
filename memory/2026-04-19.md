# 2026-04-19 Session Memory

## Alygn Infrastructure Recon (18:47-19:30 CST)

### Three Systems Checked

1. **Municipal Outreach** — Scripts exist (`test-pipeline.js`, `test-pipeline-live.js`) but **Supabase DNS fails**. `uwusstfgikzeryvaruuk.supabase.co` doesn't resolve even via Google DNS (8.8.8.8). Project likely paused or deleted on Supabase's end. Need to check Supabase dashboard.

2. **VC Outreach** — Notion API works (200 OK), 5 VCs in database (Khosla, Character, Bloomberg Beta, SV Angel, First Round). `draft-outreach-emails.js` can run (Notion-only). `send-approved-emails.js` needs SMTP (works) + Supabase (blocked by DNS). Partially operational.

3. **X Master Automation** — **Fully down.** Three problems:
   - X API v1.1 (OAuth1): 401 Unauthorized
   - X API v2 (Bearer): 403 Forbidden
   - Browser relay: Gateway timeout starting alygn Chrome profile
   - Account `@aialyygn` is suspended. Need to appeal or create new app + update credentials.

### Tailscale + Nginx Issues Found

**Problem 1: Tailscale DNS health warning** — "can't reach configured DNS servers" — cosmetic, external DNS works via systemd-resolved fallback. Fix: add global nameservers (8.8.8.8, 1.1.1.1) in Tailscale admin console.

**Problem 2: Nginx allowlist blocks remote Ollama access** — Port 11435 only allows `100.66.199.80` (self), `192.168.1.11` (old), `127.0.0.1`. Missing `100.115.234.1` (andler-pro) and entire Tailscale subnet.

**Fix prepared (requires sudo):**
```nginx
# Replace specific IPs with:
allow   100.64.0.0/10;    # Tailscale CGNAT range (all tailnet devices)
allow   192.168.1.0/24;   # Local network
allow   127.0.0.1;        # localhost
```

**Documentation created:** `docs/infrastructure/tailscale-nginx-setup.md` — full setup guide with network architecture, nginx config, SSL certs, troubleshooting, and the allowlist fix.

**Notion update started** but session ended before completion — need to finish updating the "Access & Credentials" page on Alygn Central Hub with the infrastructure docs.

### Key Insight
Supabase project `uwusstfgikzeryvaruuk` doesn't resolve on ANY DNS (Google, Cloudflare, system). This means the project is almost certainly paused or deleted. Need Andler to log into Supabase dashboard to check/restore.

## Category G GitHub Reports (from earlier session)

All 6 Category G issues reported and closed on `AndlerRL/andler-ops`:
- G-089 (#89): Config Management — Zod schema, 3 env presets, singleton loader
- G-094 (#94): LB Health/Monitoring — /health, /ready, /metrics (Prometheus)
- G-095 (#95): Resource Monitoring — CPU/memory/disk alerts, 503 at critical
- G-096 (#96): Cost Optimization — Redis op tracking, cost estimation, throttling
- G-097 (#97): Doc Synchronization — scripts/sync-docs.ts, Notion + README sync
- G-098 (#98): Incident Response Runbooks — automated detection + response

Also created (then closed) 6 duplicate issues on wrong repo (`Intention-Alliance/alygn-core-infra` #7-#12). These were cleaned up.

## Next Steps
- [ ] Apply nginx allowlist fix (sudo required)
- [ ] Fix Tailscale DNS (add nameservers in admin console)
- [ ] Check Supabase project status (paused/deleted?)
- [ ] Update Notion with infrastructure docs
- [ ] Restore X automation (unsuspend account or create new app)
- [ ] Restart OpenClaw gateway for browser relay fix