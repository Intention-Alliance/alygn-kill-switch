# Lib

Dashboard client-side libraries: `api-client.ts` (typed calls to the Training
Framework Core via `/api/*` rewrites), `auth-client.ts` (Better-Auth super-admin
session), and shared types/utilities. Mirrors `apps/web-regulator/lib/`.
