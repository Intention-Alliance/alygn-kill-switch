# Kill Switch API Configuration

# Database
DATABASE_URL=./data/db.sqlite

# Better Auth
# Generate this with: openssl rand -base64 32
BETTER_AUTH_SECRET=change-me-to-a-real-32-char-secret

# Admin Credentials
ADMIN_EMAIL=admin@alygn.com
KILL_SWITCH_AUTH_TOKEN=change-me-to-a-strong-token

# Server
PORT=3000
NODE_ENV=development
