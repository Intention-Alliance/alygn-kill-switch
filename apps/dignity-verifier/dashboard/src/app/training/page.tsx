/**
 * Training Executor — placeholder.
 * Gimglich builds the full UI. Scaffold only.
 */

export default function TrainingPage() {
  return (
    <main className="min-h-screen bg-slate-950 px-6 py-16 text-slate-100">
      <h1 className="text-3xl font-bold text-emerald-300">Training Executor</h1>
      <p className="mt-3 text-slate-400">
        Start/stop LoRA fine-tune runs, view progress + logs. Under construction.
      </p>
    </main>
  );
}
