/**
 * Reports Viewer — placeholder.
 * Gimglich builds the full UI. Scaffold only.
 */

export default function ReportsPage() {
  return (
    <main className="min-h-screen bg-slate-950 px-6 py-16 text-slate-100">
      <h1 className="text-3xl font-bold text-emerald-300">Reports</h1>
      <p className="mt-3 text-slate-400">
        Training run history, eval results, accuracy charts, distillation logs. Under construction.
      </p>
    </main>
  );
}
