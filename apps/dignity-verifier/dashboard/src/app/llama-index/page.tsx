/**
 * LlamaIndex Config — placeholder.
 * Gimglich builds the full UI. Scaffold only.
 */

export default function LlamaIndexPage() {
  return (
    <main className="min-h-screen bg-slate-950 px-6 py-16 text-slate-100">
      <h1 className="text-3xl font-bold text-emerald-300">LlamaIndex Config</h1>
      <p className="mt-3 text-slate-400">
        Augmentation params, teacher model selection, run augmentation. Under construction.
      </p>
    </main>
  );
}
