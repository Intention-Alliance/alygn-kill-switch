# Components

Reusable React components for the dashboard: dataset tables/forms, training-run
controls, augmentation config forms, and report charts. Shared UI primitives
mirror `apps/web-regulator/components/`.
