# Dashboard Source

Next.js App Router source. `app/` holds the route pages (dataset, training,
llama-index, reports), `components/` holds reusable UI, `lib/` holds API
clients, auth, and shared utilities. Follows the `apps/web-regulator` structure.
