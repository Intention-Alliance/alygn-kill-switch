{
  "name": "@align/decision-core",
  "version": "1.0.0",
  "description": "DecisionProvider implementations (keyword/ollama/jev/remote) + fail-closed selector for the kill-switch",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
